name: My first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: welcome message
              run: echo "my first github actions job, te amo melissa!"

            - name: list files
              run: ls            

            - name: read file
              run: cat README.md
